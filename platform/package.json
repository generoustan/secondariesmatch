{
  "name": "@secondariesmatch/platform",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 0/1 technical substrate for SecondariesMatch: deal graph, event spine, mandate matching, pricing evidence engine, diligence checklist, clause-locked document generation, deterministic verification, entitlements, and the audit ledger.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
